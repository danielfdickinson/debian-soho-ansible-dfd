[sshd]
#mode = normal
{% if (instance_item['sshd_port'] is defined | ternary(instance_item['sshd_port'], edata.sshd_port)) != 22 %}
port = ssh,{{ instance_item['sshd_port'] is defined | ternary(instance_item['sshd_port'], edata.sshd_port) }}
{% else %}
port = ssh
{% endif %}
enabled = true
