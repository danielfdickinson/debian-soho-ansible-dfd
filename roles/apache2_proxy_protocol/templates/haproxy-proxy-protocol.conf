<IfModule mod_remoteip.c>
	RemoteIPProxyProtocol On

	# Requests to any server using this configuration must have a PROXY protocol
	# header provided. If it is missing, the connection will be aborted.
</IfModule>
# vim: syntax=apache2
