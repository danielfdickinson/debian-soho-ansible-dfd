# Apache2 service role

* Configures and enables one vhost
* Reloads apache2 on changes (a restart may still be triggered if there are
dependences on modules that result in changes to the module situation).
