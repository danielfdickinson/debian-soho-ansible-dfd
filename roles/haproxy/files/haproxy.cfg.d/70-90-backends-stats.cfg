backend be_server_stats
	stats enable
	stats uri /server-stats
	stats hide-version
	stats refresh 5s

	acl AUTH http_auth(stats-auth)
	acl AUTH_ADMIN http_auth_group(stats-auth) admin
	stats http-request auth realm gate-stats unless AUTH
	stats admin if AUTH_ADMIN
